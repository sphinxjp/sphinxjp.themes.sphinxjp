language: python
sudo: false
cache:
  directories:
    - $HOME/.cache/pip
python:
  - "2.7"
  - "3.5"
  - "3.6"
install:
  - pip install -U pip setuptools
  - pip install -r requirements.txt tox
script:
  - tox
